<% project = Project.find(project_id) %>

<% project.addendums.each do |addendum| %>
  <%= render '/addendum_versions/form', addendum_version: addendum.most_recent_addendum_version, editable: editable  %>

  <br><br>
<% end %>
