<% project = Project.find(project_id) %>

<% project.addendums.each do |addendum| %>
  <%= render '/addendum_versions/show', addendum_version_id: addendum.most_recent_addendum_version.id, editable: editable  %>

  <br><br>
<% end %>
