<tr>
  <% form_id = random_id %>
  <%= form_with(url: configuration_path, method: :patch, id: form_id) do |form| %>
    <td>
      Marketplace::StrongerStyle
      <%= question_tooltip('Section titles often use this style') %>
    </td>
    <td>
      <%= configuration_size_tag(:marketplace_stronger_style_size, Setting.marketplace_stronger_style_size, form_id) %>
    </td>
    <td>
      <%= configuration_weight_tag(:marketplace_stronger_style_weight, Setting.marketplace_stronger_style_weight, form_id) %>
    </td>
    <td>
      <%= configuration_face_tag(:marketplace_stronger_style_face, Setting.marketplace_stronger_style_face, form_id) %>
    </td>
    <td>
      <%= configuration_color_tag(:marketplace_stronger_style_color, 'Color', Setting.marketplace_stronger_style_color, form_id)  %>
    </td>
  <% end %>
</tr>
