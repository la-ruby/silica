<div id="activity_<%=project.id%>">
  <% project.events_for_activity_tab.each do |event| %>
    <% next unless event.activityble? %>
    <%= render partial: '/events/activity', locals: { event: event } %>
  <% end %>
</div>
