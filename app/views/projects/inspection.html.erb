<% if UxPolicy.new(current_user, nil).inspection? %>
  <%= render '/shared/summary', project: @project %>

  <%= render '/shared/tabs', project: @project, active: 'Inspection' %>

  <div class="mt-3"><!-- spacer --></div>

  <%= render '/shared/inspection_inner_tabs', project: @project, active: 'Overview' %>


  <%= render '/component/inspection_overview_pane', project_id: @project.id %>
<% end %>
