<% if UxPolicy.new(current_user, nil).inspection? %>
  <%= render '/projects/show/shared/summary', project: @project %>

  <%= render '/projects/show/shared/tabs', project: @project, active: 'Inspection' %>

  <div class="mt-3"><!-- spacer --></div>

  <%= render '/shared/inspection_inner_tabs', project: @project, active: 'Overview' %>


  <%= render '/scouts/inspection_overview_pane', project_id: @project.id %>
<% end %>
